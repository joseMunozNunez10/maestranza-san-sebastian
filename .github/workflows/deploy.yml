# ===================================================================
# DEPLOYMENT via cPanel Git Version Control
# ===================================================================
# 
# Este workflow está deshabilitado porque usamos cPanel Git para 
# deployment automático.
#
# El deployment se realiza automáticamente cuando se hace push 
# a la rama 'main' mediante cPanel Git Version Control.
#
# Configuración actual:
# - Repository: /home1/maestr67/repositories/maestranza-san-sebastian
# - Deploy path: ~/public_html/
# - Archivo de config: .cpanel.yml
#
# ===================================================================

name: Disabled - Using cPanel Git

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: |
          echo "Este workflow está desactivado."
          echo "Usamos cPanel Git para deployment automático."
